# 🤖 Настройка Telegram бота

## Создание бота через BotFather

### Шаг 1: Найди BotFather

1. Открой Telegram
2. Найди бота `@BotFather` (официальный бот от Telegram)
3. Отправь `/start`

### Шаг 2: Создай нового бота

1. Отправь команду `/newbot`
2. BotFather попросит ввести **имя бота** (отображаемое имя):
   ```
   SatWave Bot
   ```
   или любое другое имя

3. Введи **username бота** (должен быть уникальным и заканчиваться на `bot`):
   ```
   satwave_analysis_bot
   ```
   Примеры других вариантов:
   - `satwave_waste_bot`
   - `my_satwave_bot`

4. BotFather создаст бота и пришлет токен

### Шаг 3: Сохрани токен

Токен выглядит примерно так:
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-123456789
```

⚠️ **ВАЖНО**: 
- Не делись токеном ни с кем
- Не коммить токен в Git
- Храни в `.env` файле

## Настройка бота

### Установка команд

Отправь @BotFather команду `/setcommands`, выбери своего бота и отправь:

```
start - Начать работу с ботом
help - Получить помощь
stats - Посмотреть статистику
```

### Установка описания

**Short Description** (отображается в списке ботов):
```
/setdescription

Анализирует фото мусора и определяет тип отходов
```

**About** (отображается в профиле бота):
```
/setabouttext

SatWave Bot помогает анализировать отходы с помощью AI.

Отправь фото мусора и геолокацию - получи:
🗑️ Тип отходов (пластик, металл, бумага и т.д.)
📊 Уровень уверенности
📍 Сохранение в базу данных

Часть экосистемы SatWave для борьбы с мусором.
```

### Установка аватарки (опционально)

```
/setuserpic
```

Загрузи квадратное изображение (рекомендуется 512x512px)

## Конфигурация в проекте

### 1. Создай файл .env

Если файла `.env` нет в корне проекта, создай его:

```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

### 2. Добавь токен

Открой `.env` и найди строку:
```bash
TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_HERE
```

Замени на свой токен:
```bash
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-123456789
```

### 3. Настрой другие параметры (опционально)

```bash
# Логирование
LOG_LEVEL=INFO  # DEBUG для детальных логов

# ML модель
ML_MODEL_TYPE=stub  # stub, yolo, detectron2

# Дедупликация
DUPLICATE_CHECK_THRESHOLD_METERS=50.0
```

## Установка зависимостей

```bash
# Установка всех зависимостей
pip install -r requirements.txt

# Или только для бота
pip install aiogram>=3.3.0
```

## Запуск бота

### Локальный запуск

```bash
# Способ 1: Через модуль Python
python -m satwave.adapters.bot.telegram_bot

# Способ 2: Через команду (если установлен проект)
pip install -e .
satwave-bot
```

Ты должен увидеть:
```
INFO - Starting Telegram bot...
INFO - Bot started polling
```

### Запуск через Docker

```bash
# Только бот
docker-compose up bot

# Или всё вместе (API + Bot + DB)
docker-compose up
```

## Проверка работы

### Базовая проверка

1. Найди своего бота в Telegram по username
2. Отправь `/start`
3. Должно прийти приветственное сообщение

### Полная проверка

1. Отправь любое фото
2. Отправь геолокацию (нажми на скрепку → Location → Share My Location)
3. Дождись ответа с результатом анализа

Пример ответа:
```
✅ Анализ завершен!

🗑️ Тип мусора: 🥤 PLASTIC
📊 Найдено объектов: 2
📍 Локация: 55.755800, 37.617300
🆔 ID анализа: 123e4567-e89b-12d3-a456-426614174000

📋 Детали:
🥤 plastic: 85%
🔩 metal: 12%
```

## Troubleshooting

### ❌ "Token is invalid"

**Проблема**: Бот не запускается, ошибка про токен

**Решение**:
1. Проверь, что токен правильно скопирован в `.env`
2. Убедись, что нет лишних пробелов
3. Проверь, что файл `.env` в корне проекта
4. Попробуй пересоздать бота через @BotFather

### ❌ Бот не отвечает

**Проблема**: Бот найден, но не отвечает на сообщения

**Решение**:
1. Убедись, что скрипт запущен
2. Проверь логи в консоли
3. Попробуй отправить `/start` еще раз
4. Проверь, что нет firewall блокировок

### ⚠️ "Duplicate location error"

**Проблема**: Бот пишет, что локация уже использовалась

**Решение**:
- Это нормальное поведение! Система помнит локации
- Отправь фото из другого места (дальше 50 метров)
- Или измени порог в `.env`: `DUPLICATE_CHECK_THRESHOLD_METERS=10.0`

### ⏳ Бот получил фото, но не обработал

**Проблема**: Отправил фото, но анализ не запустился

**Решение**:
- Отправь геолокацию! Бот ждёт и фото, и координаты
- Порядок не важен: можно сначала локацию, потом фото

### 🐛 Другие проблемы

Включи debug-логи:
```bash
# В .env
LOG_LEVEL=DEBUG

# При запуске
python -m satwave.adapters.bot.telegram_bot
```

Смотри логи в консоли для деталей.

## Дополнительные настройки

### Webhook вместо Polling (для продакшна)

По умолчанию бот использует **polling** (постоянно спрашивает Telegram о новых сообщениях).

Для продакшна лучше использовать **webhook**:

```python
# В telegram_bot.py
async def start(self) -> NoReturn:
    await self.bot.set_webhook(
        url=f"https://yourdomain.com/webhook/telegram",
        secret_token="your_secret"
    )
    await self.dp.start_webhook(...)
```

### Rate Limiting

Для защиты от спама добавь middleware:

```python
from aiogram.utils.chat_action import ChatActionMiddleware

dp.message.middleware(ChatActionMiddleware())
```

### Admin-команды

Добавь команды только для администраторов:

```python
ADMIN_IDS = [123456789]  # Твой Telegram ID

@router.message(Command("admin"))
async def admin_panel(message: Message):
    if message.from_user.id not in ADMIN_IDS:
        await message.answer("Доступ запрещен")
        return
    # Админ логика
```

## Следующие шаги

- [Пользовательские сценарии](user-flows.md) - Как пользователи работают с ботом
- [Разработка бота](development.md) - Добавление новых функций
- [API документация](../api/overview.md) - Альтернативный способ отправки данных
