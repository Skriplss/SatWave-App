# 🏗️ Архитектура SatWave

## Общий обзор

SatWave построен на принципах **Clean Architecture** (Чистая архитектура) с разделением на слои:

```
┌─────────────────────────────────────────────────┐
│            External Interfaces                   │
│  (Telegram Bot, REST API, CLI, Web UI)          │
└────────────────┬────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────┐
│              Adapters Layer                      │
│  • API (FastAPI)                                │
│  • Bot (Aiogram)                                │
│  • Storage (PostgreSQL/PostGIS)                 │
│  • ML (YOLOv8, Detectron2)                      │
└────────────────┬────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────┐
│           Application Layer                      │
│  • PhotoAnalysisService                         │
│  • WasteClassificationService                   │
│  • LocationValidationService                    │
└────────────────┬────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────┐
│              Domain Layer                        │
│  • PhotoAnalysis                                │
│  • Location                                     │
│  • WasteDetection                               │
│  • Business Rules                               │
└─────────────────────────────────────────────────┘
```

## Структура проекта

```
src/satwave/
├── core/                    # Доменная логика (никаких зависимостей)
│   ├── domain/
│   │   ├── models.py       # Сущности: Location, PhotoAnalysis
│   │   ├── exceptions.py   # Бизнес-исключения
│   │   └── ports.py        # Интерфейсы (абстракции)
│   └── services/
│       └── photo_analysis_service.py  # Use cases
│
├── adapters/               # Адаптеры к внешнему миру
│   ├── api/                # REST API (FastAPI)
│   │   ├── app.py
│   │   ├── webhook.py
│   │   └── dependencies.py
│   ├── bot/                # Telegram Bot (Aiogram)
│   │   ├── telegram_bot.py
│   │   └── handlers.py
│   ├── storage/            # БД и файловое хранилище
│   │   ├── stub_repository.py
│   │   └── stub_photo_storage.py
│   └── ml/                 # ML модели
│       └── stub_classifier.py
│
└── config/                 # Конфигурация
    └── settings.py         # Pydantic Settings
```

## Слои и ответственность

### 1. Domain Layer (Доменный слой)

**Ответственность**: Бизнес-логика и правила

**Файлы**:
- `models.py` - сущности домена
- `exceptions.py` - доменные исключения
- `ports.py` - интерфейсы для адаптеров

**Правила**:
- ❌ Не зависит от фреймворков
- ❌ Не знает о БД, API, ML моделях
- ✅ Только чистая бизнес-логика
- ✅ 100% покрытие тестами

**Пример**:
```python
@dataclass
class Location:
    latitude: float
    longitude: float
    
    def __post_init__(self) -> None:
        # Валидация - это бизнес-правило
        if not -90 <= self.latitude <= 90:
            raise ValueError(f"Invalid latitude: {self.latitude}")
```

### 2. Application Layer (Слой приложения)

**Ответственность**: Use cases (сценарии использования)

**Файлы**:
- `photo_analysis_service.py` - обработка фото

**Правила**:
- ✅ Оркестрирует доменные объекты
- ✅ Использует порты (интерфейсы)
- ❌ Не знает о конкретных реализациях
- ✅ Тестируется с моками

**Пример**:
```python
class PhotoAnalysisService:
    def __init__(
        self,
        photo_storage: IPhotoStorage,  # Интерфейс!
        classifier: IWasteClassifier,   # Интерфейс!
    ):
        ...
```

### 3. Adapters Layer (Слой адаптеров)

**Ответственность**: Интеграция с внешним миром

**Компоненты**:
- **API** - REST endpoints (FastAPI)
- **Bot** - Telegram интерфейс (Aiogram)
- **Storage** - БД и файлы (PostgreSQL, S3)
- **ML** - Модели классификации (YOLOv8)

**Правила**:
- ✅ Реализуют интерфейсы из портов
- ✅ Конвертируют данные между внешним миром и доменом
- ✅ Могут быть заменены без изменения core

### 4. Configuration Layer

**Ответственность**: Настройки приложения

**Файлы**:
- `settings.py` - Pydantic Settings

## Dependency Injection

Используем **функциональный DI** через FastAPI:

```python
def get_photo_analysis_service() -> PhotoAnalysisService:
    settings = get_settings()
    
    photo_storage = get_photo_storage(settings)
    analysis_repo = get_analysis_repository(settings)
    waste_classifier = get_waste_classifier(settings)
    
    return PhotoAnalysisService(
        photo_storage=photo_storage,
        analysis_repo=analysis_repo,
        waste_classifier=waste_classifier,
    )
```

## Принципы

### SOLID

- **S**ingle Responsibility - один класс = одна ответственность
- **O**pen/Closed - открыт для расширения, закрыт для модификации
- **L**iskov Substitution - интерфейсы взаимозаменяемы
- **I**nterface Segregation - много маленьких интерфейсов
- **D**ependency Inversion - зависим от абстракций, не от реализаций

### Чистая архитектура

1. **Независимость от фреймворков** - можно заменить FastAPI на что угодно
2. **Тестируемость** - core тестируется без БД/API
3. **Независимость от БД** - можно переключиться с PostgreSQL на MongoDB
4. **Независимость от UI** - можно добавить Web UI без изменений в core

## Поток данных

### Входящий запрос (Inbound)

```
Telegram/API → Adapter → Service → Domain
```

1. Пользователь отправляет данные
2. Adapter (Bot/API) принимает и валидирует
3. Service обрабатывает (use case)
4. Domain выполняет бизнес-логику

### Исходящий запрос (Outbound)

```
Domain → Port (Interface) → Adapter → External System
```

1. Service нужно сохранить данные
2. Вызывает Port (интерфейс)
3. Adapter реализует Port
4. Данные сохраняются в БД/S3

## Тестирование архитектуры

### Unit тесты (Domain)

```python
def test_location_validation():
    with pytest.raises(ValueError):
        Location(latitude=100.0, longitude=0.0)
```

### Integration тесты (Adapters)

```python
def test_webhook_photo_upload(client: TestClient):
    response = client.post("/webhook/photo", ...)
    assert response.status_code == 201
```

### Acceptance тесты (End-to-End)

```python
async def test_full_analysis_flow():
    # Отправка фото → обработка → сохранение → ответ
    pass
```

## Преимущества архитектуры

✅ **Модульность** - легко добавлять новые адаптеры
✅ **Тестируемость** - core не зависит от внешних систем
✅ **Масштабируемость** - можно разделить на микросервисы
✅ **Поддерживаемость** - понятная структура и разделение

## Недостатки

⚠️ **Бойлерплейт** - больше файлов и интерфейсов
⚠️ **Сложность для новичков** - нужно понимать слои
⚠️ **Overhead** - для простых проектов может быть избыточно

## Дальнейшее развитие

### Фаза 1 (текущая)
- [x] Базовая структура
- [x] Stub адаптеры

### Фаза 2
- [ ] Реальная БД (PostgreSQL)
- [ ] Реальные ML модели (YOLOv8)
- [ ] S3 для фото

### Фаза 3
- [ ] Микросервисная архитектура
- [ ] Event-driven подход
- [ ] CQRS для аналитики

## См. также

- [ADR-001: Clean Architecture](../adr/001-clean-architecture.md)
- [Алгоритм анализа](analysis-flow.md)
- [Сервисы](services.md)

