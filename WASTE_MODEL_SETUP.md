# 🗑️ Настройка модели для детекции мусора

## Проблема

Текущая реализация использует **базовую YOLOv8l модель**, обученную на **COCO dataset** (80 классов: person, car, dog, cup, bottle и т.д.).

**COCO dataset НЕ содержит классов для мусора**, поэтому модель не может правильно определять типы мусора.

## Решения

### Вариант 1: Использовать готовую модель для waste detection (РЕКОМЕНДУЕТСЯ)

#### Roboflow Universe
1. Зарегистрируйся на [Roboflow](https://roboflow.com)
2. Найди модель для waste detection (например, "Trash Detection" или "Waste Classification")
3. Экспортируй модель в формате YOLOv8
4. Скачай веса модели (`.pt` файл)
5. Укажи путь в `.env`:
   ```env
   ML_MODEL_PATH=./models/waste-detection.pt
   ```

#### Hugging Face
1. Поищи модели на [Hugging Face](https://huggingface.co/models?search=yolov8+waste)
2. Скачай веса модели
3. Укажи путь в `.env`

### Вариант 2: Дообучить модель на своем датасете

#### Шаги:
1. **Собери датасет**:
   - Фото мусора разных типов (пластик, бумага, металл и т.д.)
   - Минимум 100-200 фото на класс

2. **Разметь данные**:
   - Используй [LabelImg](https://github.com/tzutalin/labelImg) или [Roboflow](https://roboflow.com)
   - Формат: YOLO (`.txt` файлы с координатами)

3. **Обучи модель**:
   ```python
   from ultralytics import YOLO
   
   # Загрузи базовую модель
   model = YOLO("yolov8l.pt")
   
   # Обучи на своем датасете
   model.train(
       data="dataset.yaml",  # Конфиг датасета
       epochs=100,
       imgsz=640,
       batch=16,
       device="cuda",  # или "cpu"
   )
   
   # Сохрани модель
   model.save("models/waste-detection.pt")
   ```

4. **Обнови маппинг классов**:
   - В `yolo_classifier.py` обнови метод `_map_yolo_class_to_waste_type()`
   - Используй классы из твоей модели

### Вариант 3: Использовать другую архитектуру

- **Detectron2** - лучше для instance segmentation
- **Custom CNN** - полный контроль, но нужно обучать с нуля

## Текущее состояние

Сейчас используется **эвристический маппинг** COCO классов в типы мусора:
- `cup` → PLASTIC
- `bottle` → PLASTIC
- `bowl` → PLASTIC
- и т.д.

**Это не работает хорошо**, потому что:
- Модель не различает одноразовую посуду от обычной
- Модель не знает про контекст "мусор"
- Многие объекты маппятся в UNKNOWN

## Рекомендации

1. **Для MVP**: Используй готовую модель с Roboflow
2. **Для продакшна**: Дообучи модель на своем датасете
3. **Для максимальной точности**: Собери большой датасет и обучи с нуля

## Полезные ссылки

- [Ultralytics YOLOv8 Docs](https://docs.ultralytics.com/)
- [Roboflow Universe](https://universe.roboflow.com/)
- [TACO Dataset](https://tacodataset.org/) - датасет мусора
- [TrashNet Dataset](https://github.com/garythung/trashnet) - еще один датасет мусора

