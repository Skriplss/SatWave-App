# 🤖 Настройка модели Roboflow Trash Sorter

## Что сделано

Интегрирована поддержка модели **YOLOv12 Trash Sorter** с Roboflow:
- Автоматическое скачивание модели с Roboflow
- Правильный маппинг классов (модель обучена на мусоре)
- Поддержка YOLOv12 через Ultralytics

## Настройка

### 1. Установи зависимости

```bash
pip install roboflow
```

Или:
```bash
pip install -r requirements.txt
```

### 2. Получи API ключ Roboflow

1. Зарегистрируйся на [Roboflow](https://roboflow.com)
2. Перейди в [Settings](https://roboflow.com/settings)
3. Скопируй API ключ

### 3. Обнови `.env` файл

Добавь API ключ в `.env`:

```env
ROBOFLOW_API_KEY=твой_api_ключ_здесь
ROBOFLOW_WORKSPACE=asts31
ROBOFLOW_PROJECT=trash-sorter-all-classes
ROBOFLOW_VERSION=1
```

### 4. Перезапусти бота

Модель скачается автоматически при первом запуске (если файла нет).

## Как это работает

1. При первом запуске бот проверяет наличие модели в `./models/`
2. Если модели нет и указан `ROBOFLOW_API_KEY` - скачивает модель с Roboflow
3. Модель сохраняется в `./models/trash-sorter-all-classes-v1.pt`
4. При следующих запусках используется скачанная модель

## Классы модели

Модель Trash Sorter обучена на следующих классах:
- `plastic` → PLASTIC
- `paper`, `cardboard` → PAPER
- `metal` → METAL
- `glass` → GLASS
- `organic`, `food` → ORGANIC
- `textile`, `clothes` → TEXTILE
- `electronics`, `electronic`, `battery` → ELECTRONICS
- `mixed`, `trash`, `waste` → MIXED

## Альтернатива: Скачать модель вручную

Если не хочешь использовать API:

1. Зайди на [Roboflow Universe](https://universe.roboflow.com/asts31/trash-sorter-all-classes)
2. Экспортируй модель в формате YOLOv12
3. Скачай веса модели (`.pt` файл)
4. Положи в `./models/`
5. Укажи путь в `.env`:
   ```env
   ML_MODEL_PATH=./models/твой_файл.pt
   ```

## Проверка

После настройки отправь фото мусора в бота - должно определять типы мусора правильно!

