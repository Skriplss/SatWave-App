# 🔧 Troubleshooting - Бот не отвечает после отправки геолокации

## Проблема

Бот получил фото, попросил геолокацию, но после отправки геолокации ничего не происходит.

## Диагностика

### 1. Проверь логи

Запусти бота с DEBUG уровнем логирования:

```bash
# В .env измени:
LOG_LEVEL=DEBUG

# Запусти бота
$env:PYTHONPATH="D:\Repository\Satwave\src"
python -m satwave.adapters.bot.telegram_bot
```

Теперь в логах будет видно:
- Получение фото
- Получение геолокации
- Вызов `process_analysis`
- Ошибки при инициализации сервиса
- Ошибки при обработке фото

### 2. Проверь .env

Убедись, что в `.env` правильно настроено:

```env
# Для тестирования (stub - быстро, без ML)
ML_MODEL_TYPE=stub

# Или для YOLOv8 (нужна модель)
ML_MODEL_TYPE=yolo
ML_MODEL_PATH=./models/yolov8n.pt
ML_MODEL_CONFIDENCE_THRESHOLD=0.5
```

### 3. Проверь, что модель существует (если используешь YOLOv8)

```bash
# Проверь, что файл существует
ls models/yolov8n.pt

# Если нет - скачай:
python scripts/download_yolo_model.py
```

### 4. Проверь импорты

Убедись, что все зависимости установлены:

```bash
pip install -r requirements.txt
```

### 5. Типичные ошибки

#### Ошибка: "Model file not found"

**Решение**:
1. Проверь путь в `.env`: `ML_MODEL_PATH=./models/yolov8n.pt`
2. Убедись, что файл существует
3. Или используй stub: `ML_MODEL_TYPE=stub`

#### Ошибка: "ultralytics package not installed"

**Решение**:
```bash
pip install ultralytics
```

#### Ошибка: "ModuleNotFoundError: No module named 'satwave'"

**Решение**:
```bash
# Установи PYTHONPATH
$env:PYTHONPATH="D:\Repository\Satwave\src"
```

#### Ошибка: "Service obtained successfully" но дальше ничего

**Проблема**: Ошибка в `service.process_photo()`

**Решение**: Проверь логи - там должна быть детальная ошибка

## Быстрое решение

### Вариант 1: Используй stub (для тестирования)

В `.env`:
```env
ML_MODEL_TYPE=stub
```

Это должно работать без проблем.

### Вариант 2: Проверь логи

Запусти с DEBUG и посмотри, что происходит:
```bash
LOG_LEVEL=DEBUG python -m satwave.adapters.bot.telegram_bot
```

## Что проверить в логах

Ищи строки:
- `"User {id} uploaded photo"` - фото получено ✅
- `"User {id} sent location"` - геолокация получена ✅
- `"Getting photo analysis service..."` - сервис инициализируется
- `"Service obtained successfully"` - сервис готов ✅
- `"Starting analysis..."` - анализ запущен ✅
- `"Analysis completed"` - анализ завершен ✅
- `"ERROR"` или `"Exception"` - ошибка ❌

Если видишь ошибку - скопируй её и покажи мне!

